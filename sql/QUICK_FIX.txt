╔══════════════════════════════════════════════════════════════════╗
║                    🔧 DATABASE FIX GUIDE                         ║
║                  Fix "relation users exists" Error               ║
╚══════════════════════════════════════════════════════════════════╝

PROBLEM: You ran the wrong SQL file (supabase-schema.sql)
SOLUTION: Clean up and run the correct file (supabase-setup.sql)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 STEP 1: CLEAN UP EXISTING TABLES (If you already ran wrong file)

Go to: Supabase Dashboard → SQL Editor
Run this cleanup script:

┌──────────────────────────────────────────────────────────────────┐
│ -- Drop tables from incorrect schema                             │
│ DROP TABLE IF EXISTS appointments CASCADE;                       │
│ DROP TABLE IF EXISTS consultations CASCADE;                      │
│ DROP TABLE IF EXISTS patients CASCADE;                           │
│ DROP TABLE IF EXISTS doctors CASCADE;                            │
│ DROP TABLE IF EXISTS users CASCADE;                              │
│                                                                   │
│ -- Drop views                                                    │
│ DROP VIEW IF EXISTS user_profiles CASCADE;                       │
│ DROP VIEW IF EXISTS consultation_queue CASCADE;                  │
│ DROP VIEW IF EXISTS upcoming_appointments CASCADE;               │
│                                                                   │
│ -- Drop functions                                                │
│ DROP FUNCTION IF EXISTS generate_consultation_id() CASCADE;      │
│ DROP FUNCTION IF EXISTS update_updated_at_column() CASCADE;      │
└──────────────────────────────────────────────────────────────────┘

⚠️  Note: If you haven't run any SQL yet, skip to Step 2

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ STEP 2: RUN THE CORRECT SQL FILE

File to use: sql/supabase-setup.sql

1. Open the file: sql/supabase-setup.sql
2. Copy ALL contents (100 lines)
3. Go to: Supabase Dashboard → SQL Editor → New Query
4. Paste the entire contents
5. Click "Run" or press Ctrl+Enter

This creates:
  ✓ profiles table (single table for patients & doctors)
  ✓ Row Level Security policies
  ✓ Auto-update trigger for timestamps

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 STEP 3: VERIFY DATABASE SETUP

Go to: Supabase Dashboard → Table Editor

You should see:
  ✅ ONE table named "profiles"
  ❌ NO tables: users, patients, doctors, consultations, appointments

Check profiles table structure:
  ✓ id (uuid, primary key)
  ✓ email, role, name, phone
  ✓ Patient fields: age, weight, height, blood_group, etc.
  ✓ Doctor fields: specialty, doctor_id, license_number, etc.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 STEP 4: TEST YOUR APPLICATION

A. Start Backend Server:
   $ cd /home/yashraj/Documents/Project/ClinRoute
   $ npm start
   
   Should run on: http://localhost:5000

B. Start Frontend (in new terminal):
   $ cd /home/yashraj/Documents/Project/ClinRoute/client
   $ npm start
   
   Should open: http://localhost:3000

C. Test Signup:
   1. Click "Get Started" on landing page
   2. Click "Sign Up" tab
   3. Fill form:
      - Email: test@example.com
      - Password: Test123456!
      - Role: Patient
      - Name: Test User
   4. Click "Sign Up"
   
   ✅ Expected: Account created, redirected to profile setup

D. Fill Profile:
   1. Enter required fields (marked with *):
      - Full Name: Test User
      - Age: 25
      - Weight: 70
      - Height: 175
   2. Click "Save Changes"
   
   ✅ Expected: No errors, "Profile complete" badge appears

E. Access Dashboard:
   1. Click "Go to Dashboard" button
   
   ✅ Expected: Redirected to patient dashboard

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ CHECKLIST: Everything Should Work Now

Database:
  ☐ Ran cleanup script (if needed)
  ☐ Ran supabase-setup.sql
  ☐ Verified "profiles" table exists

Environment:
  ☐ .env has SUPABASE_URL and SUPABASE_ANON_KEY
  ☐ client/.env has REACT_APP_SUPABASE_URL and REACT_APP_SUPABASE_ANON_KEY

Application:
  ☐ Backend running on port 5000
  ☐ Frontend running on port 3000
  ☐ Can create account
  ☐ Can save profile (no "invalid input" error)
  ☐ Can access dashboard

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❓ STILL HAVING ISSUES?

Error: "invalid input syntax for type numeric"
  → Already fixed in AuthContext.js
  → Make sure you saved the file
  → Restart frontend server

Error: "profiles does not exist"
  → Run sql/supabase-setup.sql again
  → Check Supabase Table Editor

Error: Login not working
  → Check browser console (F12) for errors
  → Verify Supabase credentials in .env files
  → Check Authentication enabled in Supabase Dashboard

Error: Cannot save profile
  → Check browser console for actual error
  → Verify RLS policies created (included in supabase-setup.sql)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 ADDITIONAL RESOURCES

Full documentation: sql/README.md
Quick reference: sql/SETUP_GUIDE.md
Database file to use: sql/supabase-setup.sql ✅
Database file to ignore: sql/supabase-schema.sql ❌

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✨ After following these steps, your application should work perfectly!
